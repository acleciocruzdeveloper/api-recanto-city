name: Criar Pull Request

on:
  pull_request:
    branch: [develop]

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml

  test:
    name: Test
    uses: ./.github/workflows/test.yml